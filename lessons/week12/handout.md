# 第 12 週 | 創世神模式：OOP 物件導向

> **給教練的戰術板 (Coach's Corner)**：
> 這是課程中最難跨越的 **「邏輯之牆」** (The Wall of Logic)。
> 學生會崩潰：「老師！為什麼要把原本好好的程式碼拆得四分五裂？」
> 答案是：為了 **擴張性 (Scalability)**。
> 如果未來要加 10 種食物、3 條蛇、5 個敵人，不寫 OOP，你會寫 `if/else` 寫到手指骨折。
> OOP 是為了讓你當 **創世神**，而不是水泥工。

---

## 🎯 本週攻略目標 (Mission Objectives)

1.  **類別 (Class)**：學會畫設計圖 (Blueprint)。
2.  **實例 (Instance)**：學會從設計圖量產物件 (Object)。
3.  **封裝 (Encapsulation)**：把髒東西 (資料與邏輯) 包在膠囊裡，保持主程式乾淨。

---

## 📊 簡報精華 (Slide Highlights)

### Slide 1: Minecraft 理論 (The Minecraft Theory)
*   **Class (類別)**：是「泥土方塊」的 **定義**。它規定了泥土是褐色的、用鏟子挖。
*   **Object (物件)**：是你放在地上的 **那 100 個** 泥土方塊。每個方塊都有自己的座標 `(x, y, z)`，但它們共用同一個靈魂。
*   **Instance (實例)**：就是那每一個獨立的方塊。

### Slide 2: OOP 三大心法
1.  **屬性 (Attribute)**：它是什麼？(Color, Position, Size) -> `self.variable`
2.  **方法 (Method)**：它能做什麼？(Move, Eat, Die) -> `def function(self):`
3.  **初始化 (Init)**：它出生時要帶什麼裝備？ -> `def __init__(self):`

### Slide 3: 為什麼要用 `self`？
*   新手最容易搞混的就是 `self`。
*   想像這是一個 **點名制度**。
*   當 Snake 1 號要移動時，它必須說 `self.x += 1`，這樣才不會動到 Snake 2 號的 x 座標。
*   **self 就是「我」**。

---

## 📝 程式碼邏輯拆解 (Code Breakdown)

### 1. 宣告藍圖：Class 結構
```python
class Snake:
    def __init__(self):
        # 出生證明
        self.body = [(400, 300)]
        self.direction = "RIGHT"
    
    def move(self):
        # 自己的身體自己動
        ...
```

### 2. 主程式的進化
原本亂七八糟的 `while True` 現在變成了詩：
```python
snake = Snake() # 創世
food = Food()   # 賜糧

while True:
    snake.move()       # 蛇啊，前進吧
    if snake.eat(food): # 蛇啊，吃吧
        score += 1
    snake.draw()       # 顯現神蹟
```
這就是 **High-Level Programming**。你是在指揮，而不是在搬磚。

---

## 🏋️‍♂️ 實戰演練 (Hands-on Labs)

### 🟢 基礎題 (Identity)
1.  **命名系統**：給 `Snake` 增加一個 `name` 屬性 (例如 "Python Viper")，並在 `draw` 的時候把名字畫在頭頂上 (像 MMORPG 一樣)。
2.  **色違版**：在 `__init__` 裡面多加一個 `color` 參數，讓你可以在創造蛇的時候決定它是紅色還是綠色。
    *   `s1 = Snake(RED)`
    *   `s2 = Snake(BLUE)`

### 🟡 進階題 (Snake Farm)
1.  **群蛇亂舞**：
    *   在 `while` 迴圈外面一口氣創造 10 條蛇，存到一個 List 裡：`snakes = [Snake() for _ in range(10)]`。
    *   在迴圈裡讓它們全部一起動。
    *   這就是 **粒子系統 (Particle System)** 的雛形。
2.  **物件導向的食物**：
    *   把 `Food` 也改成 Class。
    *   讓食物有不同的 `type` (普通、黃金、毒藥)，並在 `snake.eat(food)` 的時候觸發不同的效果。

---

## 📚 寶藏連結 (Reference)
*   [Real Python OOP Guide](https://realpython.com/python3-object-oriented-programming/) (講得很細，適合想深造的人)
*   [Why OOP in Games?](https://news.ycombinator.com/item?id=30545934) (Hacker News 上的深度討論)

# 第 04 週 | 變數的超能力：記住蛇的位置

> **給老師的備課筆記 (Lesson Notes)**：
> 這是最難教的數學概念之一：「`x = x + 1`」。請準備一個粉筆盒（代表變數），在裡面放一張寫著數字的紙條。每一秒鐘，拿出紙條，加 1，再放回去。這就是「賦值 (Assignment)」。
> 本週的 `demo.py` 實作了著名的 "DVD Bouncing Logo Effects"，學生會非常喜歡看到方塊撞到牆壁變色的瞬間。

---

## 🎯 本週教學目標 (Learning Objectives)

1.  **變數 (Variable)**：理解變數就是一個有名稱的盒子，用來裝數字。
2.  **賦值 (Assignment)**：理解 `=` 不是相等，而是「把右邊的結果放進左邊的盒子」。
3.  **自動移動 (Auto-Move)**：利用 `position += speed` 讓物體動起來。
4.  **基礎碰撞 (Basic Collision)**：利用 `if` 判斷牆壁位置，實現反彈效果。

---

## 📊 教學投影片大綱 (Slides Outline)

### Slide 1: 誰偷走了我的方塊？
*   **提問**：上週我們畫的方塊永遠停在原地。如果要讓它動，該怎麼辦？
*   **答案**：每一秒鐘，偷偷把它的座標改一點點。
*   **關鍵**：我們需要一個變數來記住「現在在哪裡」。

### Slide 2: 等號的謊言 (The Lie of the Equals Sign)
*   **數學課**：`x = x + 1` (無解，老師會生氣)。
*   **程式課**：`x = x + 1` (超讚，這是讓腳色走路的魔法)。
*   **口訣**：先算右邊，再塞回左邊。

### Slide 3: 速度向量 (The Speed Vector)
*   **X 軸移動**：`x = x + speed_x`
    *   `speed_x` 是正數 -> 向右跑
    *   `speed_x` 是負數 -> 向左跑
*   **Y 軸移動**：`y = y + speed_y`
    *   `speed_y` 是正數 -> 向下掉 (重力?)
    *   `speed_y` 是負數 -> 向上飛

### Slide 4: 撞牆了怎麼辦？ (Bouncing Logic)
*   **情境**：方塊跑出視窗外面了！(x > 800)
*   **Vibe Coding 解法**：讓它彈回來！
*   **邏輯**：如果撞牆，就把速度「反轉」。
    *   `speed_x = -speed_x` (原本 +5 變 -5)
    *   就像鏡子裡的自己。

---

## 📝 程式碼邏輯拆解 (Code Breakdown)

### 1. 初始變數設定
```python
block_x = 400  # 起點在中間
speed_x = 5    # 每幀移動 5 像素
```
這些變數必須寫在 `while True` **外面**。如果寫在裡面，每次迴圈都會被重置回 400，方塊就會永遠卡在原地發抖。

### 2. 更新位置 (Update)
```python
block_x = block_x + speed_x
```
這行程式碼每一秒執行 60 次 (FPS)。
400 -> 405 -> 410 -> 415...
這就是動畫的原理。

### 3. 反彈條件 (Boundary Check)
```python
if block_x > 800 or block_x < 0:
    speed_x = -speed_x
```
*   `800` 是視窗右邊界。
*   `0` 是視窗左邊界。
*   `-speed_x` 把正速度變負，負速度變正，完美實現來回跑。

---

## 🏋️‍♂️ 動手試試看 (Hands-on Exercises)

### 🟢 基礎題 (The Walker)
1.  **改變速度**：把 `speed_x` 改成 20，看看方塊跑多快？改成 1 呢？
2.  **改變方向**：把 `speed_y` 改成 0，讓方塊只在水平方向移動 (像乒乓球)。

### 🟡 進階題 (The DVD Challenge)
1.  **重力模擬 (Gravity)**：
    *   試著在迴圈裡加入一行：`speed_y = speed_y + 0.1`
    *   觀察方塊會發生什麼事？(它會像拋物線一樣掉下來！)
2.  **變色龍**：
    *   修改程式碼，讓方塊在每次撞牆時，隨機變成一種新的顏色。(提示：使用 `random.choice`)

---

## 📚 參考資源 (References)
*   [DVD Screensaver Loop](https://www.youtube.com/watch?v=D-aQbJ898rQ) (經典梗圖，不用解釋)
*   [Pygame Rect 文檔](https://pyga.me/docs/ref/rect.html)

# 第 11 週 | 死亡宣告：You Died (狀態管理)

> **給教練的戰術板 (Coach's Corner)**：
> 死亡在遊戲中不是終結，而是 **學習循環 (Learning Loop)** 的一部分。
> 學生會在這裡學到 **狀態機 (State Machine)** 的雛形，這對未來寫大型 App (例如登入前/登入後) 非常重要。
> 貪食蛇最經典的死法是 **「咬到自己」**，這需要極快的演算法：`if x in list`。

---

## 🎯 本週攻略目標 (Mission Objectives)

1.  **狀態管理 (State Management)**：學會切換遊戲模式 (`Menu` / `Play` / `Game Over`)。
2.  **清單搜尋 (List Lookup)**：利用 `if new_head in body` 瞬間判斷自殺。
3.  **封裝重置 (Reset Function)**：如何讓遊戲重來而不需要關掉視窗 (那是爛遊戲的做法)。

---

## 📊 簡報精華 (Slide Highlights)

### Slide 1: 紅綠燈理論 (Traffic Control)
*   **想像一下**：你的遊戲邏輯被一個紅綠燈控制。
*   **綠燈 (STATE_PLAY)**：蛇一直在動，你可以控制方向。
*   **紅燈 (STATE_GAMEOVER)**：蛇死掉了，畫面凍結，不再更新位置，只偵測「重來鍵」。
*   **關鍵**：我們需要一個變數 `game_state` 來記錄現在的燈號。

### Slide 2: 咬舌自盡 (Ouroboros)
*   **問題**：蛇怎麼知道自己咬到自己？
*   **暴力解法**：用 `for` 迴圈檢查身體的每一節，看座標是否跟頭一樣。 (慢)
*   **Pythonic 解法**：
    ```python
    if new_head in snake_body:
        # 抓到了！自殺！
    ```
    *   利用 Python 強大的 `in` 關鍵字，一行搞定。

### Slide 3: Vibe Coding - 死亡儀式
*   當玩家失敗時，不要只是顯示一行冷冰冰的字。我們要 **羞辱** (誤)... 我們要 **震撼** 他。
*   **畫面變色**：整個背景瞬間轉成暗紅色 `(50, 0, 0)`。
*   **時間暫停**：死掉的那一瞬間，把 FPS 降到 0，製造出「時間停止」的絕望感。

---

## 📝 程式碼邏輯拆解 (Code Breakdown)

### 1. `reset_game()` 的藝術
為什麼我們需要寫一個 `reset_game()` 函數？
因為當遊戲結束要重來時，我們必須手動把所有變數歸零：
*   蛇變回 3 節。
*   分數變回 0。
*   方向變回向右。
如果漏了任何一個，玩家就會在下一局發現「咦？我怎麼一出生就有 1000 分？」。

### 2. 狀態機 (State Machine) 實作
```python
if game_state == "PLAY":
    update_snake()
    draw_game()
elif game_state == "GAME_OVER":
    draw_game_over_screen()
```
這就是所有遊戲引擎 (Unity, Unreal) 的核心架構。
在這個架構下，你可以輕易加入 `PAUSE` (暫停) 狀態。

---

## 🏋️‍♂️ 實戰演練 (Hands-on Labs)

### 🟢 基礎題 (Invincible)
1.  **無敵護身符**：設定一個變數 `invincible_frames = 60`。當蛇剛重生的前 1 秒內 (60 幀)，就算撞到牆也不會死。給新手一點活路。
2.  **暫停功能**：試著加入 `STATE_PAUSE`。當按下 `P` 鍵時，遊戲畫面凍結，再次按下 `P` 鍵恢復。

### 🟡 進階題 (High Score System)
1.  **永久排行榜**：
    *   雖然還沒教檔案讀寫，但你能讓程式記錄這一次執行期間的「最高分」嗎？
    *   `if score > high_score: high_score = score`。
2.  **死亡回放 (Kill Cam)**：(超難挑戰)
    *   在死掉的瞬間，不要馬上跳出 Game Over。
    *   讓蛇從尾巴開始一節一節慢慢消失 (`pop`)，直到只剩下一顆頭，再顯示 Game Over。
    *   這叫做 **Juicy Game Design**。

---

## 📚 寶藏連結 (Reference)
*   [Game State Management Patterns](https://gameprogrammingpatterns.com/state.html) (遊戲設計模式聖經)
*   [Dark Souls "YOU DIED" Analysis](https://www.youtube.com/watch?v=MDu6HjM7xSs) (為什麼這個畫面這麼經典？)

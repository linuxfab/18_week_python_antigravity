# 高一新生的 Vibe Coding 學習日誌：Week 12

## 📅 日期：2026-02-15
## 👤 角色：熱血的高一新鮮人

---

### 🤯 邏輯之牆：為什麼要把好好的程式拆爛？

這週我差點崩潰。老師說要把我們寫了 11 週的程式碼全部「砍掉重練」。
因為我們要進入傳說中的 **OOP (物件導向)**。

一開始我真的很不爽：「明明 `head_x` 和 `head_y` 用得好好的，為什麼要變成 `self.body[0]`？」
代碼量不但沒變少，反而變多了（多了一堆 `class` 設定）。
但當我寫完 `class Snake` 並且在主程式裡打下這行的時候：

```python
snake = Snake()
```

我突然懂了。
這感覺不像在寫指令，像是在 **「創世」**。
我不再是那個要親自去搬磚塊的工人，我是那個說「要有蛇」的神。

---

### 🏗️ 藍圖與實體：Minecraft 的方塊理論

老師用 Minecraft 當比喻，我就秒懂了。
*   **Class (類別)**：就是遊戲裡的「物品欄說明書」。它規定了泥土方塊長怎樣、怎麼挖。
*   **Instance (實體)**：就是我實際放要在地上的「那一個」方塊。

以前我只有一條蛇，所以沒感覺。
但如果我想做一個「百蛇亂舞」的特效，以前我可能要宣告 `snake1_x`, `snake2_x`... 寫到死。
現在？只要寫一個迴圈：
```python
snakes = [Snake() for _ in range(100)]
```
一百條蛇就誕生了！這就是 OOP 的 **擴張性 (Scalability)** 嗎？太神了吧。

---

### 🤖 `self` 是什麼？就是「我」啊！

剛開始寫 Class 的時候，每一行都要加 `self`，覺得超煩。
`self.x`, `self.y`, `self.direction`...
後來才發現，這個 `self` 就像是每個物件的「身分證」。
當有 100 條蛇同時在動的時候，Snake A 說「我要往左轉」，電腦怎麼知道是哪條蛇要轉？
就是靠 `self`。
Snake A 喊的是 `self.direction = LEFT`，所以只有它自己會轉，Snake B 根本聽不到。
這就是 **封裝 (Encapsulation)** 的安全感。

---

### 📜 主程式變成了一首詩

最讓我驚豔的是現在的 `main loop`。
以前裡面充滿了數學計算和邏輯判斷，現在變成這樣：

```python
while running:
    snake.move()
    if snake.eats(food):
        score += 1
    snake.draw()
```

這根本不是程式碼，這是英文句子吧！
讀起來超級順暢，連我那個不懂程式的隔壁桌同學都看得懂這在幹嘛。
原來 **High-Level Programming** 就是這種感覺——優雅、乾淨、像是在指揮交響樂。

---

### 🏆 十二週里程碑：創世神資格考

| 週次 | 技能 | 學習靈感 |
|:----:|:----|:--------|
| 01-04 | 基礎 | 聽命行事 |
| 05-11 | 進階 | 管理邏輯 |
| **12** | **物件導向** | **成為造物主 (Architect)** |

**目前的 Vibe 值：🔥🔥🔥🔥🔥🔥🔥🔥🔥 (創世神力覺醒)**

---

> **Repo 連結**: [linuxfab/18_week_python_antigravity](https://github.com/linuxfab/18_week_python_antigravity)
> **最後更新時間**: 2026-02-15 21:30

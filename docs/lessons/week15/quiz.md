# Week 15 | 隨堂測驗 (Quiz)：難度曲線與心流設計

> **測驗目標**：檢測學生對難度曲線公式、FPS 控制以及心流 (Flow) 理論的理解。
> **測驗時間**：10 分鐘

---

## 🟢 第一部分：是非題 (True/False)

1.  (  ) 心流理論 (Flow State) 告訴我們，遊戲應該保持極高難度，讓玩家隨時感到挫敗才有挑戰性。
2.  (  ) 在 Pygame 中，`clock.tick(fps)` 的參數 `fps` 可以是一個變數，讓我們根據分數動態調整速度。
3.  (  ) 為了防止難度無限增長導致玩家無法反應，我們應該為 FPS 設定一個上限 (MAX_FPS)。
4.  (  ) 線性增長的難度公式指的是：分數越高，難度增加的速度就會越來越慢。
5.  (  ) 當技能 (Skill) 超過挑戰 (Challenge) 太多的時候，玩家通常會感到無聊 (Boredom)。

---

## 🟡 第二部分：程式碼填空 (Code Completion)

請填入正確的代碼，計算出動態的 FPS 數值。

```python
BASE_FPS = 10
MAX_FPS = 30

def calculate_speed(score):
    # 1. 基礎難度公式：每得 5 分，FPS 就加 1
    new_fps = BASE_FPS + (score // ______)
    
    # 2. 限制最大速度 (防止速度失控)
    if new_fps > ______ :
        new_fps = ______
    
    return new_fps

# 3. 在遊戲迴圈中套用速度
while running:
    # ... 遊戲邏輯 ...
    target_speed = calculate_speed(score)
    clock.______(target_speed)
```

---

## 🔴 第三部分：腦力激盪 (Logic Challenge)

1.  **公式設計**：如果你想設計「前段簡單、後段突然變超快」的難度曲線，你會建議用什麼樣的數學運算？
    *   答案：____________________ (例如：平方、指數運算、梯級式跳躍等)

2.  **心理學應用**：除了調整「移動速度」，還有什麼方法可以讓玩家在分數變高時感到更強大的「心理壓迫感」？
    *   答案：____________________ (例如：更換快節奏 BGM、縮小視野、背景顏色變紅、縮小食物生成時間等)

---

## 💡 解答與解析

### 是非題
1. **False** (挑戰應該剛好比技能高一點點)
2. **True**
3. **True**
4. **False** (線性增長是指增長速度恆定)
5. **True**

### 程式碼填空
1. `5`
2. `MAX_FPS`, `MAX_FPS`
3. `tick`

### 腦力激盪
1. 開放性建議：指數成長 `score ** 2` 或 階梯式升級（達到特定分數直接暴增）。
2. 開放性建議：BGM 變快、螢幕震動、UI 文字閃爍、視野範圍變小 (Fog of War)。

# 第 04 週 | 變數的超能力：記住蛇的位置

> **給教練的戰術板 (Coach's Corner)**：
> 這週我們要面對一個新手大魔王：「`x = x + 1`」。
> 在數學老師眼裡這是錯的 (0 = 1 ?!)，但在程式設計師眼裡，這是讓東西 **動起來** 的咒語。
> 準備好你的粉筆盒（變數），我們要在盒子裡裝數字，並且不斷透過 Game Loop 修改它。
> 本週 Demo 是經典的 **DVD Bouncing Logo** 效果，學生看到方塊撞牆反彈變色那一刻，通常會興奮得尖叫。

---

## 🎯 本週攻略目標 (Mission Objectives)

1.  **變數 (Variable)**：理解變數就是一個有名稱的「置物櫃」，用來存放狀態。
2.  **賦值 (Assignment)**：徹底搞懂 `=` 不是「相等」，而是「把右邊算完的結果，塞進左邊的格子」。
3.  **自動導航 (Auto-Move)**：利用 `position += speed` 讓物體自己跑起來。
4.  **物理模擬 (Basic Collision)**：利用 `if` 判斷牆壁邊界，實現「撞牆反彈」。

---

## 📊 簡報精華 (Slide Highlights)

### Slide 1: 誰偷走了我的方塊？
*   **回顧**：上週我們畫的方塊像釘子一樣釘在螢幕上。
*   **目標**：我們要讓它自己長腳跑掉。
*   **關鍵**：我們需要一個變數來記住「它現在在哪裡」。因為電腦的金魚腦如果不記下來，下一秒就忘了。

### Slide 2: 等號的謊言 (The Lie of the Equals Sign)
*   **數學課**：`x = x + 1` -> **無解** (Error)。
*   **程式課**：`x = x + 1` -> **進化** (Evolution)。
*   **Vibe Coding 口訣**：**先算右邊，再塞回左邊**。
    *   拿出 x 目前的值，加 1，然後把新的結果放回 x 盒子裡。

### Slide 3: 速度向量 (Velocity Vector)
*   **X 軸移動**：`x = x + speed_x`
    *   `speed_x` 是正數 (+5) -> 向**右**衝刺
    *   `speed_x` 是負數 (-5) -> 向**左**倒退嚕
*   **Y 軸移動**：`y = y + speed_y`
    *   `speed_y` 是正數 -> 向**下**墜落 (重力?)
    *   `speed_y` 是負數 -> 向**上**升天

### Slide 4: 撞牆了怎麼辦？ (Bouncing Logic)
*   **情境**：方塊跑太爽，衝出視窗外面了！(x > 800)
*   **Vibe Solution**：給它裝個彈簧！
*   **邏輯**：如果 `x` 碰到牆壁...
    *   `speed_x = -speed_x` (原本往右跑 +5，瞬間變成往左跑 -5)
    *   這就是物理引擎的基礎。

---

## 📝 程式碼邏輯拆解 (Code Breakdown)

### 1. 初始變數設定：要在迴圈外面！
```python
block_x = 400  # 起點
speed_x = 5    # 速度
while True:
    ...
```
**警告**：這些變數必須寫在 `while True` **外面**。
如果你寫在裡面，每次迴圈 `block_x` 都會被重置回 400，你的方塊就會像卡到陰一樣在原地抽搐。

### 2. 位置更新 (Update)：動畫的原理
```python
block_x = block_x + speed_x
```
這行程式碼每一秒執行 60 次 (FPS)。
400 -> 405 -> 410 -> 415...
這就是動畫。動畫其實就是一連串快速改變的靜態圖。

### 3. 反彈機制 (Boundary Check)
```python
if block_x > 800 or block_x < 0:
    speed_x = -speed_x
```
*   `800` 是視窗右邊界 (牆壁)。
*   `0` 是視窗左邊界。
*   `or` 是邏輯判斷：不管撞到左邊**還是**右邊，都要反彈。
*   `-speed_x` 是靈魂：讓正變負，負變正。

---

## 🏋️‍♂️ 實戰演練 (Hands-on Labs)

### 🟢 基礎題 (The Walker)
1.  **速度調節**：把 `speed_x` 改成 `20`。哇！超音速！再改成 `1`，變樹懶。
2.  **方向控制**：把 `speed_y` 改成 `0`，讓方塊只在水平方向移動 (像經典的 Pong 遊戲)。

### 🟡 進階題 (The DVD Challenge)
1.  **重力模擬 (Gravity Simulator)**：
    *   試著在迴圈裡加入一行神秘代碼：`speed_y = speed_y + 0.1`
    *   觀察方塊會發生什麼事？(提示：它會開始加速掉落，就像有地心引力一樣！)
2.  **變色龍 (Chameleon)**：
    *   修改程式碼，讓方塊在 **每次撞牆時**，隨機變成一種新的顏色。
    *   使用 `random.choice` 從顏色列表裡挑一個。這會有很棒的視覺效果！

---

## 📚 寶藏連結 (Reference)
*   [DVD Screensaver 傳說](https://www.youtube.com/watch?v=D-aQbJ898rQ) (看不懂這個梗的請面壁思過)
*   [Pygame Rect 官方文檔](https://pyga.me/docs/ref/rect.html)

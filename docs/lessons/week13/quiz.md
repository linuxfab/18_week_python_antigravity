# Week 13 | 隨堂測驗 (Quiz)：色彩學與動態效果

> **測驗目標**：檢測學生對 HSV 色彩空間、`sin` 函式應用及視覺特效原理的理解。
> **測驗時間**：10 分鐘

---

## 🟢 第一部分：是非題 (True/False)

1.  (  ) 在 HSV 色彩模型中，「H (Hue)」代表的是顏色的亮度，數值越高越亮。
2.  (  ) `math.sin(time)` 函式的回傳值永遠會在 -1 到 1 之間擺盪，非常適合用來製作呼吸燈效果。
3.  (  ) 如果要製作彩虹漸層效果，比起調整 RGB 三個數值，調整 HSV 中的 Hue 會更直觀且容易。
4.  (  ) 電腦繪圖中的「Alpha」通道通常指的是顏色的透明度。
5.  (  ) 為了讓方塊看起來有發光效果，我們可以先畫一個較大且半透明的同色方塊在底部，再畫原本的實心方塊。

---

## 🟡 第二部分：程式碼填空 (Code Completion)

請完成這段製作「彩虹蛇」的程式碼。

```python
import colorsys

snake_length = 10

for i in range(snake_length):
    # 1. 計算色相 (0.0 ~ 1.0)
    # 讓顏色均勻分佈在彩虹光譜上
    hue = i / ______
    
    # 2. 將 HSV 轉換為 RGB (因為 pygame 只吃 RGB)
    # 飽和度(S)與亮度(V)設為最大值 1.0
    r, g, b = colorsys.______(hue, 1.0, 1.0)
    
    # 3. 轉換為 0~255 的整數
    color = (int(r * 255), int(g * 255), int(b * 255))
    
    # 繪製方塊...
```

---

## 🔴 第三部分：腦力激盪 (Logic Challenge)

1.  **數學應用**：如果我想讓一個物件的大小在 20 到 40 之間來回縮放，利用 `sin` 函式 (範圍 -1~1)，公式應該怎麼寫？
    *   答案：`size = 30 + math.sin(time) * ______`

2.  **視覺優化**：除了讓蛇變彩虹色，請舉出一個能讓遊戲畫面從「工程師風格」變成「藝術家風格」的小技巧。
    *   答案：____________________ (例如：加入背景網格的微光、蛇移動時留下的殘影軌跡、吃東西的粒子特效...)

---

## 💡 解答與解析

### 是非題
1. **False** (Hue 是色相，Value 才是亮度)
2. **True**
3. **True**
4. **True**
5. **True**

### 程式碼填空
1. `snake_length` (或 `len(snake_body)`)
2. `hsv_to_rgb`

### 腦力激盪
1. `10` (因為 sin*10 範圍是 -10~10，加上 30 後就是 20~40)
2. 開放性建議：動態背景、螢幕震動 (Screen Shake)、粒子系統 (Particles)、光暈 (Bloom/Glow)。

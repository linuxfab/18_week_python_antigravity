# Week 09 | 隨堂測驗 (Quiz)：碰撞檢測與成長機制

> **測驗目標**：檢測學生對座標碰撞判斷 (`if ==`)、Boolean 旗標的使用，以及蛇身成長邏輯的理解。
> **測驗時間**：10 分鐘

---

## 🟢 第一部分：是非題 (True/False)

1.  (  ) 在網格系統中，判斷碰撞最精準的方法是檢查兩者的座標是否「完全相同」。
2.  (  ) 蛇之所以會變長，是因為我們在每吃一個食物後，執行了兩次 `append()`。
3.  (  ) `should_grow` 這種用來記錄狀態的變數，通常稱為「旗標 (Flag)」。
4.  (  ) 為了增加打擊感 (Juiciness)，我們可以在碰撞發生時瞬間改變畫面螢幕背景色。
5.  (  ) `(40, 40) == (40, 40)` 在 Python 裡的判斷結果是 False。

---

## 🟡 第二部分：程式碼填空 (Code Completion)

請填入正確的邏輯，讓蛇吃掉食物後能夠變長。

```python
# 1. 檢查碰撞
if head_x ______ food_x and head_y ______ food_y:
    # 2. 設定成長旗標
    should_grow = ______
    spawn_food()

# 更新身體
snake_body.append((head_x, head_y))

# 3. 處理成長邏輯
if should_grow:
    should_grow = ______ # 用完要重置
else:
    # 沒吃到就刪掉尾巴，長度不變
    snake_body.______(0)
```

---

## 🔴 第三部分：腦力激盪 (Logic Challenge)

1.  **打擊感設計**：除了背景閃動，你覺得還有什麼方式可以增加「吃掉食物」的爽感？
    *   答案：____________________ (例如：音效、震動、噴出粒子效果...)

2.  **防呆邏輯**：為什麼在 `spawn_food()` 裡要檢查食物座標是否 `in snake_body`？
    *   答案：____________________ (例如：防止食物直接生在蛇身上，讓玩家不知所措或覺得有 Bug)

---

## 💡 解答與解析

### 是非題
1. **True** (在 Grid System 下是首選)
2. **False** (是因為「不執行」pop(0))
3. **True**
4. **True**
5. **False** (內容相同，結果為 True)

### 程式碼填空
1. `==`, `==`
2. `True`
3. `False`, `pop`

### 腦力激盪
1. 開放性建議：播放 "Ding" 的音效、蛇頭產生震動、食物消失時產生小火花。
2. 避免食物一出生就跟蛇身重疊，導致食物瞬間消失或不合邏輯的生成。

# 高一新生的 Vibe Coding 學習日誌：Week 07

## 📅 日期：2026-02-15
## 👤 角色：熱血的高一新鮮人

---

### 🍝 拒絕義大利麵程式碼 (Spaghetti Code)

直到上週為止，我的 `main.py` 已經腫得像一坨打結的耳機線。
所有的邏輯——移動、判斷邊界、畫蛇、畫背景——全部塞在那個 `while True` 迴圈裡。
每次要改一個顏色，都要滑鼠滾輪滾半天，眼睛都花了。

老師說這叫 **「技術債 (Technical Debt)」**，如果不還，遲早會破產。
今天的課不教新功能，而是教我們 **「整理房間」**。
一開始覺得很無聊，「能跑就好了幹嘛改？」
但當我學會 `def` 之後，我跪了。

---

### 🧙‍♂️ 自創咒語：`def` 的魔力

今天學到的 `def` (Definition) 讓我感覺自己像個魔法師，可以創造自己的咒語。

以前要畫蛇，我要寫一大串 `for` 迴圈加 `draw.rect`。
現在我把它打包起來：
```python
def draw_snake():
    # ... 把那堆髒活塞進來 ...
```

然後在主程式裡只要喊一聲：
```python
draw_snake()
```
電腦就乖乖去畫了！
我的主迴圈瞬間從 100 行變成了 10 行，看起來超級清爽：
1. `draw_grid()`
2. `draw_snake()`
3. `flip()`

這就是傳說中的 **Abstraction (抽象化)** 嗎？感覺大腦終於可以呼吸了。

---

### 🌐 賽博網格 (Cyber Grid)：80 年代的浪漫

除了整理程式碼，今天還加了一個視覺大升級：**背景網格**。
原本黑漆漆的背景，加上了深灰色的線條後，整個空間感都出來了！
有一種《Tron》或者 80 年代機臺遊戲的復古科幻感 (Synthwave Vibe)。

寫法也很聰明，用 `range(0, 800, 40)` 讓電腦自己算座標，不用傻傻地寫 20 行 `draw.line`。
我試著把網格顏色改成 **Neon Cyan (螢光青)**，那個視覺衝擊力簡直了！搭配上週的彩虹蛇，現在我的螢幕看起來就像是駭客任務的母體。

---

### 🔧 重構 (Refactoring)：為了走更長的路

老師說：「重構是不改變功能，只改變結構。」
我原本以為這是多此一舉，但現在我懂了。
如果沒有把 `draw_snake` 獨立出來，我根本不敢想之後要怎麼加「食物」或者「計分板」。
現在把房間整理乾淨了，才有空間放新的傢俱。

寫程式不只是在寫邏輯，更是在設計一種 **架構之美**。
今天的 Vibe 不是來自於畫面有多炫，而是來自於看到潔淨程式碼的那種 **秩序感 (Order)**。

---

### 🏆 七週里程碑

| 週次 | 技能 | 心得 |
|:----:|:----|:----|
| 01-02 | 基礎 | 召喚視窗與方塊 |
| 03-04 | 動態 | 讓它動起來、彈起來 |
| 05-06 | 邏輯 | 操控它、讓它變長 |
| **07** | **架構** | **讓程式碼變優雅** |

**目前的 Vibe 值：🔥🔥🔥🔥🔥 (程式碼潔癖覺醒)**

---

> **Repo 連結**: [linuxfab/18_week_python_antigravity](https://github.com/linuxfab/18_week_python_antigravity)
> **最後更新時間**: 2026-02-15 20:30

# 高一新生的 Vibe Coding 學習日誌：Week 06

## 📅 日期：2026-02-15
## 👤 角色：熱血的高一新鮮人

---

### 🚃 從口袋升級到火車：List 的衝擊

之前幾週，我都是用 `block_x`、`block_y` 這種「單一變數」在記東西。
老師說那叫「口袋」——一個口袋只能裝一個東西。

但今天！我們拿到了一整列 **火車** `[]`。
```python
snake_body = []
```
這短短一行，代表了一條可以無限延伸的車廂。
每一節車廂裡裝的是一組座標 `(x, y)`，這叫**元組 (Tuple)**——兩個數字永遠黏在一起，像連體嬰一樣不能拆。

老師話一說完我就想到多啦A夢的百寶袋，什麼東西都塞得進去。
但 List 比百寶袋更厲害，因為它是**有順序的**——先放的在前面，後放的在後面。

---

### 🐛 毛毛蟲移動法：排隊的藝術

今天最炸裂的觀念是蛇的移動原理。
我一直以為蛇在移動的時候，是「每一節身體都在跑」。
但其實真相是：

1. **頭**往前長出一節新的 → `snake_body.append(new_head)`
2. **尾巴**縮掉最舊的一節 → `snake_body.pop(0)`
3. **中間的身體根本沒動！**

這就像排隊買便當——前面的人買完離開，後面來新的人排隊。
隊伍看起來在「移動」，但其實中間的人根本沒有位移。
我被這個概念徹底驚艷了。原來這叫 **FIFO (先進先出)**。

接著我做了一個瘋狂實驗：把 `pop(0)` 那行註解掉。
結果蛇變成了一條 **永遠不會消失的光牆**，就像電影《Tron: 光速戰記》裡的光束機車！
原來只要移除那一行 `pop`，貪食蛇就變成了另一種遊戲。

---

### 🌈 彩虹蛇：我的螢幕變成了夜店

視覺效果是今天的最大 Vibe。
老師用了一個叫 **HSV 色彩模型** 的東西，讓蛇的每一節都有不同的顏色：

```python
def get_rainbow_color(index, total):
    hue = index / total
    r, g, b = colorsys.hsv_to_rgb(hue, 1.0, 1.0)
    return (int(r*255), int(g*255), int(b*255))
```

簡單來說，**色相 (Hue)** 從 0 到 1 走一圈就是完整的彩虹。
我們把每一節身體的索引值 (index) 除以總長度，就能讓蛇身從頭到尾剛好跑完一整圈色譜。

更瘋的是**動態大小**：尾巴那一端 (index=0) 變成一個小點，頭部則是正常大小。
這讓整條蛇看起來像一顆**彗星拖著尾巴**在飛！
看著那條彩虹蛇在漆黑的背景上蜿蜒滑行，我旁邊同學說我的螢幕看起來像夜店的雷射燈 😂

---

### 📝 隨堂測驗心得：差點踩進陷阱

今天還有一個小測驗 (quiz.md)。
我差點在是非題第 3 題翻車：

> `snake_body.pop(0)` 會刪除清單中**最新**加入的元素。

我手滑差點選 True，因為直覺以為 `pop` 就是「把最後面的拿走」。
但仔細看，`pop(0)` 裡面有個 `0`，代表的是刪除**第 0 個** (最前面/最舊的) 元素！
如果是 `pop()` 不帶參數，才是刪除最後面的。

腦力激盪題也很有趣——我在紙上手動推演了 `append` 跟 `pop` 的過程：
```
原始: [(10,10), (20,10), (30,10)]
append(40,10) → [(10,10), (20,10), (30,10), (40,10)]
pop(0)        → [(20,10), (30,10), (40,10)]
蛇頭 = (40,10) ← 最後加入的就是頭！
```
這種「在紙上跑程式」的練習，比光看代碼有用太多了。

---

### 🔮 六週里程碑

| 週次 | 超能力 | 一句話 |
|:----:|:------|:------|
| 01 | 視窗 + 事件 | 召喚視窗 |
| 02 | 座標 + 形狀 | 放置方塊 |
| 03 | FPS + 動畫 | 方塊呼吸 |
| 04 | 變數 + 反彈 | 方塊自走 |
| 05 | 操控 + 慣性 | 坐上駕駛座 |
| **06** | **List + FIFO** | **蛇終於有身體了！** |

**目前的 Vibe 值：🌈🌈🌈🌈🌈 (彩虹模式解鎖)**

---

> **Repo 連結**: [linuxfab/18_week_python_antigravity](https://github.com/linuxfab/18_week_python_antigravity)
> **最後更新時間**: 2026-02-15 20:10

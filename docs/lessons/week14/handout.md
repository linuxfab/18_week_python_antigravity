# 第 14 週 | 聽覺盛宴：聲音的靈魂 (Sound FX)

> **給教練的戰術板 (Coach's Corner)**：
> 聲音是遊戲 **回饋迴圈 (Feedback Loop)** 的最後一塊拼圖。
> 科學研究顯示：有音效的按鈕，使用者會覺得「反應更靈敏」。
> 哪怕畫面再爛，只要音效配得好，遊戲就成功了一半 (想想恐怖遊戲)。
> 重點：理解 Mixer 的 **非同步 (Asynchronous)** 特性 —— 程式不會停下來等聲音播完。

---

## 🎯 本週攻略目標 (Mission Objectives)

1.  **音響工程師 (Mixer)**：理解同步與異步播放，以及 Channel 的概念。
2.  **格式之爭**：為什麼音效要用 `.wav`，音樂要用 `.mp3`？
3.  **防禦性程式設計**：防止因為少了一個音效檔就讓整個遊戲崩潰 (Crash)。

---

## 📊 簡報精華 (Slide Highlights)

### Slide 1: 錄音機 vs 現場樂團
*   **音效 (Sound Effect/SFX)**：
    *   像是「按鈕印章」。按一下，發出一次聲音。
    *   **特性**：短、快、多。可以同時疊加 (吃到食物 + 撞牆)。
    *   **格式**：WAV (無壓縮，反應最快)。
*   **音樂 (Background Music/BGM)**：
    *   像是「現場樂團」。你只能有一組樂手在台上。
    *   **特性**：長、循環 (Loop)、串流 (Streaming)。
    *   **格式**：MP3/OGG (壓縮，省硬碟空間)。

### Slide 2: 載入資源的正確姿勢
*   **錯誤**：`s = Sound("boom.wav")` -> 如果檔案不見或是檔名打錯，程式直接當掉。
*   **正確 (Pro)**：
    ```python
    try:
        eat_sound = pygame.mixer.Sound("assets/eat.wav")
    except:
        print("警告：找不到音效檔，啟用靜音模式")
        eat_sound = None  # 設為 None，之後用 if 判斷
    ```
    *   這叫 **Graceful Degradation (優雅降級)**。

### Slide 3: Vibe Coding - 立體聲 (Stereo)
*   你的耳朵能分辨聲音是從左邊來還是右邊來。
*   **Panning (相位)**：根據食物在螢幕的左邊或右邊，動態調整左右聲道的音量。
*   `sound.set_volume(left, right)`

---

## 📝 程式碼邏輯拆解 (Code Breakdown)

### 1. 初始化 Mixer
```python
pygame.mixer.init(frequency=44100, size=-16, channels=2, buffer=512)
```
*   `buffer` (緩衝區) 是關鍵。
*   設太大：聲音會有延遲 (按下按鍵 0.5 秒才響)。
*   設太小：聲音會破音 (滋滋聲)。
*   `512` 通常是 FPS 遊戲的黃金數值。

### 2. 精準觸發
```python
if head == food:
    if eat_sound: 
        eat_sound.play() # Fire and Forget (射後不理)
```
*   `play()` 是非阻塞的。它只是告訴音效卡「嘿，排進去播」，然後 CPU 繼續去算下一行程式碼。

---

## 🏋️‍♂️ 實戰演練 (Hands-on Labs)

### 🟢 基礎題 (DJ Snake)
1.  **背景音樂**：找一首你喜歡的 8-bit 音樂，讓它在遊戲開始時無限循環播放 (`loops=-1`)。
2.  **吃播主**：為「吃食物」和「撞牆」加上音效。

### 🟡 進階題 (Sound Designer)
1.  **音調隨機化 (Pitch Variation)**：
    *   如果你每次吃到東西的聲音都一模一樣，耳朵會疲勞 (Ear Fatigue)。
    *   **挑戰**：準備三個頻率略有不同的 `pico1.wav`, `pico2.wav`, `pico3.wav`，放入一個 list，每次吃的時候 `random.choice()` 挑一個播。
    *   這是所有專業遊戲 (如 Minecraft) 採礦聲的秘密。
    *   *註：Pygame 的 Sound object 本身不支援即時變調，所以我们要用多個檔案來作弊。*
2.  **BGM 加速**：
    *   當蛇長度 > 10，切換成另一首更快的背景音樂 (或是把音樂設為 1.2 倍速，如果使用的音樂庫支援的話)。

---

## 📚 寶藏連結 (Reference)
*   [Kenny Assets](https://kenney.nl/assets/impact-sounds) (免費、高品質的遊戲音效庫，獨立開發者必備)
*   [Freesound.org](https://freesound.org/) (挖寶的好地方)
*   [Audacity](https://www.audacityteam.org/) (免費的剪音效軟體)
